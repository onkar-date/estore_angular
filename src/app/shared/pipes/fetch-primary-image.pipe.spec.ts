import { FetchPrimaryImagePipe } from './fetch-primary-image.pipe';

describe('FetchPrimaryImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FetchPrimaryImagePipe();
    expect(pipe).toBeTruthy();
  });
});
