import { PaiseToRupeePipe } from './paise-to-rupee.pipe';

describe('PaiseToRupeePipe', () => {
  it('create an instance', () => {
    const pipe = new PaiseToRupeePipe();
    expect(pipe).toBeTruthy();
  });
});
