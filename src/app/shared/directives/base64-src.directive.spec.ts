import { Base64SrcDirective } from './base64-src.directive';

describe('Base64SrcDirective', () => {
  it('should create an instance', () => {
    const directive = new Base64SrcDirective();
    expect(directive).toBeTruthy();
  });
});
